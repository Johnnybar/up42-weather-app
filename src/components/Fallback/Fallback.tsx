import React from "react";
import "./Fallback.scss";

const message = "Please wait for page to load";

const Fallback = () => <div className="weather-app-fallback">{message}</div>;

export default Fallback;
